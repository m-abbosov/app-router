import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Blog Page',
  description: 'Client-Side Rendered Todos',
};

const Blog = () => {
  return <div>Blog</div>;
};

export default Blog;
